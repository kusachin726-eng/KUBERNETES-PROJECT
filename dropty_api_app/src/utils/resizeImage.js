// import Resizer from "react-image-file-resizer";

// export const ResizeImage = (file, imgWidth, imgHeight, imgType="JPEG", quality="100") => new Promise(resolve => {
//     Resizer.imageFileResizer(file, imgWidth, imgHeight, imgType, quality, 0,
//     uri => {
//       resolve(uri);
//     }, 'base64' );
// });